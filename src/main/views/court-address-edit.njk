{% extends "template.njk" %}
{% from "govuk/components/fieldset/macro.njk" import govukFieldset %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}

{# MACROS #}

{% macro aolSelection(areasOfLaw, selectedIds) %}
  {% set areaOfLawItems = [] %}
  {% for area in areasOfLaw | sort(false, true, 'name') %}
    {% set areaOfLawItems = (areaOfLawItems.push({
      value: area.id,
      text: area.name,
      checked: selectedIds and area.id in selectedIds
    }), areaOfLawItems) %}
  {% endfor %}
  {{ govukCheckboxes({
    idPrefix: "areas-of-law",
    name: "areas-of-law",
    classes: "govuk-checkboxes--small app-checkboxes--two-columns",
    fieldset: {
      legend: {
        text: "Areas of law"
      }
    },
    hint: {
      text: "For example, this address is only used for sending documents about civil cases. Select all that apply."
    },
    items: areaOfLawItems
  }) }}
{% endmacro %}

{% macro courtTypeSelection(courtTypes, selectedIds) %}
  {% set courtTypeItems = [] %}
  {% for type in courtTypes | sort(false, true, 'name') %}
    {% set courtTypeItems = (courtTypeItems.push({
      value: type.id,
      text: type.name,
      checked: selectedIds and type.id in selectedIds
    }), courtTypeItems) %}
  {% endfor %}
  {{ govukCheckboxes({
    idPrefix: "court-types",
    name: "court-types",
    classes: "govuk-checkboxes--small app-checkboxes--two-columns",
    fieldset: {
      legend: {
        text: "Court types"
      }
    },
    hint: {
      text: "For example, one or more court types apply for this one address. Select all that apply."
    },
    items: courtTypeItems
  }) }}
{% endmacro %}

{# END MACROS #}

{% block pageTitle %}
  {{ pageTitle }}
{% endblock %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <h2 class="govuk-heading-l">Address</h2>

      {% set actionUrl %}
        {% if addressId %}
          /courts/{{ courtId }}/edit/address/details/success/{{ addressId }}
        {% else %}
          /courts/{{ courtId }}/edit/address/details/success
        {% endif %}
      {% endset %}

      <form action="{{ actionUrl }}" method="post">

        {% call govukFieldset({
          legend: {
            text: "Enter an address",
            classes: "govuk-fieldset__legend--m",
            isPageHeading: false
          }
        }) %}

          {{ govukRadios({
            idPrefix: "address-description",
            name: "addressType",
            fieldset: {
              legend: {
                text: "Select what applies"
              }
            },
            items: [
              { value: "VISIT_US", text: "Visit", checked: address.addressType == "VISIT_US" },
              { value: "WRITE_TO_US", text: "Send documents to", checked: address.addressType == "WRITE_TO_US" },
              { value: "VISIT_OR_CONTACT_US", text: "Visit and send documents to", checked: address.addressType == "VISIT_OR_CONTACT_US" }
            ]
          }) }}

          {{ govukInput({
            label: {
              text: "Address line 1"
            },
            id: "address-line-1",
            name: "addressLine1",
            autocomplete: "address-line1",
            value: address.addressLine1
          }) }}

          {{ govukInput({
            label: {
              text: "Address line 2 (optional)"
            },
            id: "address-line-2",
            name: "addressLine2",
            autocomplete: "address-line2",
            value: address.addressLine2
          }) }}

          {{ govukInput({
            label: {
              text: "Town or city"
            },
            classes: "govuk-!-width-two-thirds",
            id: "address-town",
            name: "townCity",
            autocomplete: "address-level2",
            value: address.townCity
          }) }}

          {{ govukInput({
            label: {
              text: "County (optional)"
            },
            classes: "govuk-!-width-two-thirds",
            id: "address-county",
            name: "addressCounty",
            value: address.county
          }) }}

          {{ govukInput({
            label: {
              text: "Postcode"
            },
            classes: "govuk-input--width-10",
            id: "address-postcode",
            name: "postcode",
            autocomplete: "postal-code",
            value: address.postcode
          }) }}

        {% endcall %}

        {% call govukFieldset({
          legend: {
            text: "Does this address only apply to a specific area of law?",
            classes: "govuk-fieldset__legend--m",
            isPageHeading: false
          }
        }) %}

          {% set aolYesChecked = true if (address.areasOfLaw and address.areasOfLaw.length > 0) else false %}
          {% set aolNoChecked = not aolYesChecked %}

          {{ govukRadios({
            idPrefix: "address-aol",
            name: "address-aol",
            items: [
              {
                value: "yes",
                text: "Yes",
                conditional: {
                html: aolSelection(areasOfLaw, address.areasOfLaw) | safe,
                checked: aolYesChecked
              }
              },
              { value: "no", text: "No", checked: aolNoChecked }
            ]
          }) }}

        {% endcall %}

        {% call govukFieldset({
          legend: {
            text: "Does this address only apply to a specific court type?",
            classes: "govuk-fieldset__legend--m",
            isPageHeading: false
          }
        }) %}

          {% set courtTypesYesChecked = true if (address.courtTypes and address.courtTypes.length > 0) else false %}
          {% set courtTypesNoChecked = not courtTypesYesChecked %}

          {{ govukRadios({
            idPrefix: "address-court-type",
            name: "address-court-type",
            items: [
              {
                value: "yes",
                text: "Yes",
                conditional: {
                  html: courtTypeSelection(courtTypes, address.courtTypes) | safe
                },
                checked: courtTypesYesChecked
              },
              { value: "no", text: "No", checked: courtTypesNoChecked}
            ]
          }) }}

        {% endcall %}

        {{ govukButton({
          text: "Save"
        }) }}

      </form>

    </div>
  </div>

  <pre>
    {{ address | dump(2) }}
    {{ errors | dump(2) }}
  </pre>
{% endblock %}
